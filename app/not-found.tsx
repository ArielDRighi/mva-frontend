import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Página no encontrada | MVA Logística y Servicios",
  description:
    "Lo sentimos, la página que buscas no existe. Podés volver al inicio o contactarnos para recibir ayuda. Si encontraste un error, por favor notificalo para que podamos solucionarlo rápidamente.",
};

const NotFoundPage = () => {
  return <>
  
  </>;
};

export default NotFoundPage;
